# Normalize line endings for all files that Git considers text files.
* text=auto eol=lf

# Only include the addons folder when downloading from the Asset Library.
/**        export-ignore
/addons    !export-ignore
/addons/** !export-ignore

# 念の為、画像ファイルも改行コードの変換を無効化
*.png   -text
*.jpg   -text